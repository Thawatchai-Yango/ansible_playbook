---
- name: Create an empty text file
  hosts: server1
  gather_facts: yes
  become: yes
  become_user: fcpo

  tasks:
    - name: Create the empty text file
      file:
        path: /home/fcpo/ansible_test/empty.txt
        state: touch
        owner: fcpo
        group: fcpo
        mode: '0777'
